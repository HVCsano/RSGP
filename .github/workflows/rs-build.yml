name: Release Crates

on:
    push:
        tags:
            - 'v*'

jobs:
    build-linux:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Rust
              uses: actions-rust-lang/setup-rust-toolchain@v1
            - name: Build crates
              run: cargo build --release
            - name: Create Release
              id: create_release
              uses: softprops/action-gh-release@v2
              with:
                  tag_name: ${{ github.ref_name }}
                  name: Release ${{ github.ref_name }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Upload worker binary
              uses: softprops/action-gh-release@v2
              with:
                  draft: true
                  files: |
                      target/release/rsgp-manager
                      target/release/rsgp-worker
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    build-windows:
        runs-on: windows-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Set up Rust
              uses: actions-rust-lang/setup-rust-toolchain@v1

            - name: Build crates
              run: cargo build --release
            - name: Create Release
              id: create_release
              uses: softprops/action-gh-release@v2
              with:
                  tag_name: ${{ github.ref_name }}
                  name: Release ${{ github.ref_name }}
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Upload worker binary
              uses: softprops/action-gh-release@v2
              with:
                  draft: true
                  files: |
                      target/release/rsgp-manager.exe
                      target/release/rsgp-worker.exe
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    build-linux-arm:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Install cross
              run: cargo install cross

            - name: Install ARM OpenSSL dev libraries
              run: sudo apt-get update && sudo apt-get install -y libssl-dev:arm64 pkg-config

            - name: Build for ARM64
              run: cross build --release --target aarch64-unknown-linux-gnu

            - name: Rename ARM binaries
              run: |
                  mv target/aarch64-unknown-linux-gnu/release/rsgp-manager target/aarch64-unknown-linux-gnu/release/rsgp-manager-arm
                  mv target/aarch64-unknown-linux-gnu/release/rsgp-worker target/aarch64-unknown-linux-gnu/release/rsgp-worker-arm

            - name: Upload ARM binaries
              uses: softprops/action-gh-release@v2
              with:
                  draft: true
                  files: |
                      target/aarch64-unknown-linux-gnu/release/rsgp-manager-arm
                      target/aarch64-unknown-linux-gnu/release/rsgp-worker-arm
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    build-web:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 20

            - name: Install dependencies
              run: npm i
              working-directory: ./web

            - name: Build web
              run: npm run build
              working-directory: ./web

            - name: Archive web build
              run: zip -r web.zip build/
              working-directory: ./web

            - name: Upload web.zip to release
              uses: softprops/action-gh-release@v2
              with:
                  draft: true
                  files: ./web/web.zip
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
